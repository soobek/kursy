<div class="hero-unit">
<h1>Edycja Artykułu:</h1><h2 style="text-align:center;"><%= @artykuly.nazwa %></h2>

<%= form_for(:artykuly, :url => {:action => 'aktualizuj', :id => @artykuly.id, :strona_id => @strony.id}) do |pole| %>

<%= render(:partial => "form", :locals => {:pole => pole}) %>

<div style="text-align: center;">
	<%= link_to("anuluj", {:action => 'index', :strona_id => @strony.id}, :class => 'btn btn-warning')%>
	<%= submit_tag("aktualizuj", :class => 'btn  btn-primary') %>
</div>

<% end %>
</div>
